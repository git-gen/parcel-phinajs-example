{
  "name": "parcel-phina-example",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "clear": "rm -rf dist && rm -rf .cache && rm -rf build",
    "dev": "yarn clear && parcel src/*.html",
    "build": "yarn clear && parcel build src/*.html -d build --public-url '.'"
  },
  "author": "gen",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "phina.js": "^0.2.3"
  }
}
